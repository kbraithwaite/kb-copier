# questions
project_name:
    type: str
    help: What is your project name?

module_name:
    type: str
    default: "{{ project_name | lower | replace(' ', '_') | replace('-', '_') }}"
    help: What is your Python module name?

fav_number:
    type: int
    help: What is your favorite number?
